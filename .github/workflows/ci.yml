name: CI

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      # `ci` rather than `install`: the lockfile is the point of running this.
      - run: npm ci

      - run: npm run typecheck

      - run: npm run lint

      - run: npm test

      # No secrets and no .env: the desktop build has to work for a stranger
      # who has never heard of Supabase, and this is what proves it.
      - run: npm run build -w @mission/desktop
